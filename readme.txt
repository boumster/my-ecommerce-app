For frontend
Modules used: react-router-dom, axios
must run the following to get started:

cd Frontend
npm install
npm run start

for backend
Modules used: json, flask, flask_cors
Must run the following to get started

cd Backend
pip install flask
pip install flask_cors
pip install json
python -m -venv -venv
venv\Scripts\activate
python app.py